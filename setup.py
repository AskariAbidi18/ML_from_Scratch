from setuptools import setup, find_packages

setup(
    name="baremetalml",         # Package name
    version="0.1",              # Version number
    packages=find_packages(),   # Automatically find all subpackages
)
